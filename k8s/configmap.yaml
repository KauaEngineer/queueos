apiVersion: v1
kind: ConfigMap
metadata:
  name: queueos-config
  namespace: queueos
data:
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  REDIS_DB: "0"
  WORKER_CONCURRENCY: "10"
  MAX_RETRIES: "3"
  JOB_TIMEOUT_MS: "30000"
  DASHBOARD_PORT: "3001"
